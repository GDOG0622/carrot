# carrot TODO

## char 主动发消息（后端驱动）

目标：char 能在你没开酒馆时主动找你说话——后端定时触发、自己调 AI 生成消息、写进聊天记录、推送到手机。浏览器开不开都不影响。

整体链路与现状：

| # | 环节 | 状态 | 说明 |
|---|------|------|------|
| 1 | 触发规则：char 什么时候主动说话 | ❌ 待做 | 后端定时器；规则可以有：固定时段、距上次聊天超过 N 小时、随机性（避免像闹钟）。需要设置 UI |
| 2 | 后端调 AI 生成消息 | ❌ 待做（最重） | 后端要拿到：API key（酒馆 secrets 服务器端可读）、模型配置、char 卡设定、近期聊天上下文，自己组 prompt 请求 AI。相当于在后端复刻一套简化版的酒馆 prompt 组装 |
| 3 | 写进酒馆聊天记录 | ❌ 待做 | 解析酒馆 jsonl 聊天文件格式，向目标 char 的当前聊天追加一条消息；注意和酒馆运行时的文件读写不冲突 |
| 4 | 开着酒馆时页面实时刷出消息 | ❌ 待做 | 前端轮询后端「有没有新主动消息」，或后端写完通知前端重载聊天 |
| 5 | 没开酒馆时推送到设备 | ✅ v8.0.23 完成 | Web Push（FCM/APNs/Mozilla 自动分流），点通知打开酒馆 |

设计时要想清楚的问题（动手前先讨论）：

- [ ] 主动消息发到哪个 char / 哪个聊天？（固定一个「常驻 char」，还是最近聊过的？）
- [ ] 触发规则想要哪些？频率上限（比如一天最多几条，别刷屏）
- [ ] 后端组 prompt 的忠实度：要不要带世界书/预设，还是只用 char 卡 + 最近 N 条上下文的简化版
- [ ] 生成失败（API 挂了/欠费）时的行为：静默跳过还是推送报错
- [ ] 多 char 支持还是先单 char 跑通

## 已完成

- [x] v8.0.23 Web Push 后端推送：零依赖 VAPID + aes128gcm，订阅管理，失效清理
- [x] v8.0.24 通知自动去重：本地系统通知优先（即时），弹不出来时后端推送兜底
- [x] v8.0.22 图片描述直插 `[描述.jpg]`；清缓存改硬刷新式；同步移入齿轮面板
