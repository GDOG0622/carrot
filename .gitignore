# 后端运行时数据（VAPID 密钥 / 推送订阅 / 缓存），不进版本库
plugin/push-data/
plugin/covers/
plugin/uploads/
node_modules/

# 内部文档，只存本地不进 GitHub
README.md
todo.md

# Claude Code 的本地配置和笔记。VPS_ACCESS_NOTE.md 里有服务器 IP / SSH 端口，
# 这是公开仓库，绝对不能进 git
.claude/
